<%= form_with(model: member, local: true) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
      <% member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :member_name %>
  </div>

  <div class="field">
    <%= form.label :age_group_id %>
    <%= form.number_field :age_group_id, id: :member_age_group_id %>
  </div>

  <div class="field">
    <%= form.label :membership_id %>
    <%= form.number_field :membership_id, id: :member_membership_id %>
  </div>

  <div class="field">
    <%= form.label :lesson %>
    <%= form.number_field :lesson, id: :member_lesson %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
